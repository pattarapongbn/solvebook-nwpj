# คู่มือใช้งานประจำวัน

สำหรับคนที่ดูแลร้าน ไม่ต้องรู้เรื่องโค้ด

| อยากทำอะไร | ไปที่ |
|---|---|
| ดูออเดอร์ ตรวจสลิป ยืนยันเงินเข้า ใส่เลขพัสดุ | `/admin/orders` |
| หาลูกค้าเก่า ดูประวัติ โหลด CSV ลบข้อมูลตามคำขอ | `/admin/customers` |
| ลิงก์ที่เอาไปแปะแอด | `/shop/` |

ครั้งแรกที่เปิดหน้า `/admin` ระบบจะขอ **โทเคนแอดมิน** ใส่ค่าเดียวกับ `ADMIN_TOKEN`
ที่ตั้งไว้ใน Render เครื่องนั้นจะจำไว้ให้ ไม่ต้องใส่ซ้ำ (ถ้าเปลี่ยนเครื่อง/ล้าง browser ต้องใส่ใหม่)

---

## งานประจำวัน — 4 ขั้น

### 1. เปิด `/admin/orders` ดูแถวที่ขึ้นว่า "แนบสลิปแล้ว"

แถวพวกนี้คือคนที่โอนเงินมาแล้วรอคุณตรวจ

### 2. ดูป้ายผลตรวจสลิปในคอลัมน์ "สลิป"

| ป้าย | แปลว่า | ต้องทำอะไร |
|---|---|---|
| **อ่าน QR ผ่าน** | ระบบอ่านเลขอ้างอิงบนสลิปได้ และไม่เคยมีใครใช้สลิปใบนี้มาก่อน | เช็คว่าเงินเข้าบัญชีจริงตามยอด แล้วกดยืนยัน |
| **อ่าน QR ไม่ได้ — ต้องตรวจเอง** | ภาพเบลอ ถ่ายจากจอ หรือไม่ใช่สลิป | **กดปุ่ม "ดูสลิป" เปิดรูปดูด้วยตา** แล้วเทียบกับยอดเงินเข้าจริง |
| **สลิปซ้ำ** | สลิปใบนี้เคยถูกใช้กับออเดอร์อื่นแล้ว | ระบบบล็อกให้แล้ว ลูกค้าต้องแนบใบใหม่ — ถ้าเจอบ่อยกับคนเดิม ให้ระวัง |

### 3. เช็คเงินเข้าแล้วกด "ยืนยันเงินเข้า"

**ยอดของทุกออเดอร์มีเศษสตางค์ไม่ซ้ำกัน** (เช่น 890.37 กับ 890.64) เปิดแอปธนาคาร
ดูยอดที่เข้ามาแล้วจับคู่กับแถวที่ยอดตรงกันเป๊ะได้เลย ไม่ต้องเดาว่าเป็นของใคร

ถ้าต่อ Zapier ไว้ ระบบจะเปลี่ยนเป็น "เงินเข้าแล้ว" ให้เองโดยไม่ต้องกด

> **อย่ากดยืนยันเพราะเห็นสลิปสวย** — สลิปปลอมทำเนียนได้ ตัวตัดสินคือเงินเข้าบัญชีจริง
> ป้าย "อ่าน QR ผ่าน" แค่บอกว่าสลิปใบนี้ไม่ซ้ำและไม่ได้ตัดต่อหยาบๆ เท่านั้น

### 4. แพ็คของ ส่ง แล้วใส่เลขพัสดุในช่องขวาสุด กด "บันทึก"

---

## เคสที่เจอบ่อย

**มีเงินเข้าแต่ไม่รู้ของใคร** — ถ้าต่อ Zapier ไว้ ยอดที่จับคู่ไม่ได้จะขึ้นเป็นกล่อง
"เงินเข้าที่จับคู่ออเดอร์ไม่ได้" บนสุดของหน้าออเดอร์ ใส่รหัสออเดอร์ (เช่น `SC260725-0001`)
แล้วกด "ผูกออเดอร์" ระบบจะยืนยันเงินเข้าให้เอง

**ลูกค้าโทรมาถามว่าของถึงไหน** — เปิด `/admin/customers` ค้นด้วยเบอร์โทร
จะเห็นประวัติการสั่งและที่อยู่ล่าสุด

**ลูกค้าโอนแล้วแต่ปิดหน้าเว็บไป** — ไม่หาย ระบบจำไว้ในเครื่องลูกค้า
เปิดลิงก์เดิมจะเด้งกลับมาหน้าจ่ายเงินพร้อมยอดเดิม

**หมดเวลา 15 นาทีแล้วลูกค้ายังไม่โอน** — กด "ขอ QR ใหม่" ในหน้าลูกค้าได้
**ยอดจะเป็นยอดเดิมเสมอ** เพราะระบบจองเศษสตางค์นั้นไว้ให้ออเดอร์นี้แล้ว

**แถวที่ขึ้น "รอโอนเงิน" เยอะ** — คือคนที่กดเข้ามาถึงหน้าจ่ายเงินแล้วหายไป
เป็นเรื่องปกติของการยิงแอด ไม่ใช่ออเดอร์ที่ต้องทำอะไร

**ลูกค้าขอให้ลบข้อมูล (PDPA)** — `/admin/customers` → ปุ่ม "ลบข้อมูล"
ระบบจะล้างชื่อ/เบอร์/อีเมลออก แต่เก็บยอดขายไว้เพื่อทำบัญชี และออเดอร์เก่า
ยังแสดงที่อยู่ที่ส่งไปจริงตอนนั้น

**อยากย้ายไปใช้ระบบอื่น** — `/admin/customers` → Export CSV ได้ทุกฟิลด์
ที่อยู่แยกช่องพร้อมยัดเข้าระบบขนส่งเจ้าไหนก็ได้ ข้อมูลเป็นของคุณ ไม่ถูกล็อกกับใคร

---

## ข้อจำกัดที่ต้องรู้

- **ไม่มีการแจ้งเตือนเวลามีออเดอร์เข้า** ต้องเปิด `/admin/orders` ดูเอง
  ถ้ายิงแอดอยู่ ควรเช็ควันละ 2–3 รอบ (อยากได้เด้งเข้า LINE ต่อ Zapier เพิ่มได้)
- **ระบบไม่ได้เช็คยอดเงินในบัญชีให้** จนกว่าจะต่อ Zapier — ปุ่ม "ยืนยันเงินเข้า"
  เชื่อคุณ 100% ว่าตรวจมาแล้ว
- **สต๊อกสินค้ายังไม่มีในระบบ** ขายเกินของที่มีได้ ต้องคุมเอง
